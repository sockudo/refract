# refract-app-recording

Production recording application for egress-driven capture workflows.

The implementation is gated by Cargo feature `app-recording` and exposes a
bounded, versioned JSON protocol documented in `docs/protocols/app-recording.md`.
