# refract-app-echo

Diagnostic echo application used for loopback validation.

The implementation is gated by Cargo feature `app-echo` and uses the versioned
JSON protocol documented in `docs/protocols/app-echo.md`.
