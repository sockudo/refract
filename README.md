# refract

`refract` is a Rust workspace for a production-grade single-edge SFU. Phase 0 establishes the repository skeleton, lint discipline, reproducible toolchain configuration, supply-chain policy, and CI automation required before protocol and media-plane implementation begins.

